# CONSTRAINTS.md — Bridge Radio

> Environment specification for Bridge Radio.
> Claude reads this at **Session Start**, before writing any code for this app.
> Overrides generic assumptions in the root `CLAUDE.md` where they conflict.

---

## 1. Identity

| Field | Value |
|---|---|
| App name | Bridge Radio (HLS music radio) |
| Origin | Built in **Google AI Studio** (Gemini); source captured into this repo 2026-06-27 |
| PM2 process | `bridge-radio` — runs the Express/tsx backend (proxy + AI lyrics) |
| Backend port | **3032** (see root `SERVER_PORTS.md`) |
| Frontend URL | `https://radio.techbridge.edu.gh/` (Plesk **root vhost**) |
| Deploy path | `/var/www/vhosts/techbridge.edu.gh/radio/` |
| Stack | React 19 + Vite 6 + TypeScript · hls.js · Tailwind 4 · Express (tsx) |

---

## 2. Architecture (how it works)

- **Player** — React + **hls.js**, plays static HLS from the stream host.
- **Streams** — static HLS on `https://ai.techbridge.edu.gh/<genre>/master.m3u8`
  (`afrobeats`, `music`, `neosoul`), generated by `ai.techbridge.edu.gh/scripts/convert_to_hls.sh`
  (ffmpeg). To add songs: drop `.m4a` in the genre dir, run the script.
- **Backend** (`server.ts`) provides two routes:
  - `GET /api/proxy?url=` — CORS proxy / m3u8 URL rewriter for the HLS host.
  - `GET /api/lyrics?track=&genre=` — server-side AI lyrics (key never hits the client).

---

## 2a. Authentication

**None.** Bridge Radio is a public HLS player with no login, no user accounts, and no
session state (`src/` has no `auth.tsx`/`AuthContext`/`AuthGate`). `/api/proxy` and
`/api/lyrics` are open endpoints; only `/api/lyrics` is Gemini-gated (503 without a
configured relay key).

## 2b. Frontend standards

- **Vite base is relative (`./`)** — correct here because the app is served at the Plesk
  **root vhost** (`radio.techbridge.edu.gh/`), not a sub-path, and has no nested client
  routes (Pattern 29 does not apply).
- `public/manifest.json` icon is a local `favicon.svg` (Pattern 33 compliant).
- `index.html` has no external CDN `<script>`/`<link>` at boot (Pattern 32 compliant) —
  no Tailwind CDN, no Google Fonts, no analytics tag, no esm.sh importmap.
- No code-splitting audit done in this pass (Pattern 31) — `pnpm build` not run.

---

## 3. Hard Constraints

1. **NEVER expose the Gemini key to the client.** No `@google/genai` import in `src/`,
   no `define` for `GEMINI_API_KEY` in `vite.config.ts`. Lyrics go through
   `/api/lyrics`, which resolves the key via the WMS proxy (root `PATTERNS.md` #11).
2. **Root vhost routing lives in `vhost_ssl.conf`, NOT `.htaccess`** — Plesk overrides
   `.htaccess` on root vhosts. SPA fallback MUST use `%{DOCUMENT_ROOT}%{REQUEST_URI}`
   (not `%{REQUEST_FILENAME}`). Canonical copy: [`docs/vhost_ssl.conf`](./docs/vhost_ssl.conf).
   See `PATTERNS.md` #15 (this is what caused the rms outage).
3. **`server.ts` is run by PM2 via `tsx`** (Pattern 13) — `tsx` is a runtime dependency,
   not devDependency. It listens on `0.0.0.0:$PORT` so Apache proxies `127.0.0.1:3032`.
4. **Package manager: pnpm.** Node v26 on the server (`nvm use 26`).

---

## 4. Required Environment Variables (Backend `.env`)

| Variable | Purpose |
|---|---|
| `PORT` | `3032` — backend listen port (matches `SERVER_PORTS.md`) |
| `NODE_ENV` | `production` — serves built `dist/`; otherwise Vite dev middleware |
| `GEMINI_PROXY_KEY` | Authenticates with the WMS Gemini proxy (production) |

> Verified against `server.ts`: there is **no** local `GEMINI_API_KEY` fallback in code —
> only `GEMINI_PROXY_KEY` is read. Without it, `/api/lyrics` returns 503 and the server
> still boots (`custody: "unconfigured"` on `/api/health`). `.env.example` lists a
> `GEMINI_API_KEY` dev-fallback line that the code does not actually implement; treat
> that line in `.env.example` as aspirational, not real.

### Known drift: `server.ts` port default vs `SERVER_PORTS.md`

`server.ts` line 43 hardcodes `Number(process.env.PORT) || 3021` (3021 is
tb-ai-english-safari's port, not bridge-radio's). `deploy.ps1`'s `$Port` parameter
correctly defaults to **3032** and passes it explicitly as an env var at PM2 start, so
the live process runs on 3032 as intended — but the in-code fallback is wrong. Harmless
today because `deploy.ps1` always sets `PORT` explicitly; would misbind if ever started
without that env var. Not fixed here (docs-only pass) — flag for a future code touch.

---

## 5. Deploy

```powershell
cd bridge-radio
.\deploy.ps1 -Build      # server-side build + rsync dist/, deploy server.ts,
                         # pnpm --prod, write vhost_ssl.conf, pm2 start/reload,
                         # then end-to-end MIME check over HTTPS
```

The radio was historically deployed **static-only**; this script moves it to the
standard node-backed model (required for `/api/lyrics`). Deploying changes the live
vhost from static to proxied — confirm before first run.

---

## 6. Pre-Delivery Gate

```
☐ No client-side key: grep -r "GoogleGenAI\|GEMINI_API_KEY" src  → empty
☐ vite.config.ts has NO define for GEMINI_API_KEY
☐ pnpm build succeeds; dist/index.html + assets present
☐ pm2 show bridge-radio → online, 0 unstable restarts
☐ GET https://radio.techbridge.edu.gh/api/lyrics?track=test → 200 JSON or 503 (not HTML)
☐ Assets serve as text/javascript over HTTPS (end-to-end MIME check)
☐ Streams still play: ai.techbridge.edu.gh/<genre>/master.m3u8 → 200
```

---

*Authored 2026-06-27 — Daniel Frempong Twum / TUC ICT*
