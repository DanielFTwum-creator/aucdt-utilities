### omniextract

- **URL**: `https://ai-tools.techbridge.edu.gh/omniextract/` (`omniextract/deploy.ps1:17` `$HEALTH_URL`; matches base path in `omniextract/server.ts:17,75,150,225,238`)
- **Port**: 3005 (`omniextract/deploy.ps1:15` `$PORT = 3005`; `omniextract/server.ts:12` default `Number(process.env.PORT) || 3005`). Matches verified live reality `SERVER_PORTS.md:27` (`| 3005 | omniextract | omniextract | Online |`) and intent ledger `PORT-REGISTRY.md:19`. No disagreement.
- **PM2 app**: `omniextract` (`omniextract/deploy.ps1:16` `$PM2_APP = 'omniextract'`; matches `SERVER_PORTS.md:27`)
- **Stack**: Frontend — React 19 + Vite + Tailwind v4, React Router, jsPDF/pdfjs-dist for document handling (`omniextract/package.json:25-30,34,46-48`). Backend — Express + tsx on Node v26 (`omniextract/server.ts:1-6`; `tsx` in `dependencies`, `omniextract/package.json:31`, satisfying Pattern 13 Rule A).
- **Auth**: Two distinct, independently-wired auth surfaces:
  1. **Public app** — own Google button + WMS OAuth relay. The client redirects to Google (referenced via `VITE_GOOGLE_CLIENT_ID`/`VITE_GOOGLE_REDIRECT_URI` exposed through `vite.config.ts:26-28`); `server.ts` handles `GET /omniextract/callback`, and relays the code→token exchange to `${WMS}/api/oauth/google/exchange` using `GEMINI_PROXY_KEY` as the WMS service credential — the app holds no Google client secret (`omniextract/server.ts:14-16,75-147`). Result is a `omniextract_user` cookie hydrated by `AuthContext` (`omniextract/server.ts:132-142`, `omniextract/src/contexts/AuthContext.tsx:41-60`).
  2. **`/admin` Staff Portal** — a separate username/password `LoginPage` (`omniextract/src/pages/LoginPage.tsx:5-72`) backed by `AuthContext.login()` → `AuthService.login()` (`omniextract/src/contexts/AuthContext.tsx:62-75`, `omniextract/src/services/AuthService.ts`).
  **Code bug (report, not fixed)**: `AuthService.ts` posts to `${API_BASE}/api/auth/login`, where `API_BASE` defaults to `http://localhost:5000` when `VITE_API_URL` is unset (`omniextract/src/services/AuthService.ts:1,10`). Port 5000 is `tuc-rms`'s backend, not omniextract's (`SERVER_PORTS.md:52`) — omniextract's own `server.ts` defines no `/api/auth/login` route at all. Unless `VITE_API_URL` is set at build time to point somewhere real, the `/admin` Staff Portal login cannot function; this looks like a copy-paste leftover from another app's auth scaffold.
- **Gemini custody**: WMS relay (Pattern 11, `POST /api/extract` on this app → `${WMS}/api/gemini/generate`). `server.ts` builds the Gemini request body from browser-extracted PDF text/image and forwards it with `X-Gemini-Proxy-Key: GEMINI_PROXY_KEY`; the API key never reaches this app (`omniextract/server.ts:19-24,154-220`). Missing `GEMINI_PROXY_KEY` returns HTTP 503 (`omniextract/server.ts:182-186`). `PATTERNS.md:985` cites OmniExtract as the reference implementation of the `/generate`-relay fleet standard.
  **Flag**: `omniextract/CONSTRAINTS.md:53,56` documents `GEMINI_API_KEY` as still "present in `.env` but not used directly ... for legacy reasons," and `omniextract/deploy.ps1:50` pre-flight-checks `.env.local` for the literal string `GEMINI_API_KEY` (aborting the deploy if absent) — this is a live requirement in the deploy script for a variable the running code never reads. Reality (`server.ts`) confirms the key is unused; the deploy script's pre-flight gate has not been updated to match. Same pre-flight also still requires `GOOGLE_CLIENT_SECRET` in `.env.local` (`omniextract/deploy.ps1:50`) even though `server.ts:14-16` explicitly states the client secret is no longer held by this app (WMS does the exchange). Worth a deploy-script cleanup pass; not changed here per scope.
- **Deploy**: `.\deploy.ps1 -Build` (server-side git clone → `pnpm install` → `vite build` → rsync `dist/` to web root, excluding backend files → `pnpm install --prod` for the backend → PM2 hard restart, `omniextract/deploy.ps1:65-155,200-206`, Pattern 23). `.env` is rebuilt with `GEMINI_PROXY_KEY` re-injected fresh from `/opt/tuc-wms/.env` (Pattern 21 BOM/CR stripping) on every deploy (`omniextract/deploy.ps1:193-198`).
- **Known gotchas**:
  - Pattern 11 — WMS Gemini relay is the reference implementation for this pattern fleet-wide (`PATTERNS.md:985`).
  - Pattern 13 — `tsx` correctly in `dependencies`; deploy step installs with `--prod` for the backend copy but the frontend build step installs full deps first (`omniextract/deploy.ps1:120-133`).
  - Pattern 21 — BOM/CR stripping applied to the extracted `GEMINI_PROXY_KEY` (`omniextract/deploy.ps1:198`).
  - Pattern 23 — hard `pm2 delete` + `pm2 start` (not reload) so a stale proxy key or stale backend code can never survive a deploy (`omniextract/deploy.ps1:200-206`).
  - Code bug (report, not fixed): `/admin` Staff Portal login points at the wrong default API base (`localhost:5000`, tuc-rms's port) and omniextract's `server.ts` has no matching `/api/auth/login` route — see Auth section above.
  - Doc/deploy-script drift (report, not fixed): deploy pre-flight still gates on `GEMINI_API_KEY` and `GOOGLE_CLIENT_SECRET` being present in `.env.local`, both of which the current `server.ts` no longer uses — see Gemini custody section above.
