### biochemai

- **URL**: `https://ai-tools.techbridge.edu.gh/biochemai/` (`biochemai/deploy.ps1:17` `$HEALTH_URL`; callback route confirms base path in `biochemai/server.ts:86`)
- **Port**: 3002 (`biochemai/deploy.ps1:15` `$PORT = 3002`; matches verified live reality `SERVER_PORTS.md:24` `| 3002 | biochemai | biochemai | Online |` and intent ledger `PORT-REGISTRY.md:17`. No disagreement — all three sources agree.)
- **PM2 app**: `biochemai` (`biochemai/deploy.ps1:16` `$PM2_APP = 'biochemai'`; matches `SERVER_PORTS.md:24`)
- **Stack**: Frontend — React 19 + Vite, Tailwind v4 (`biochemai/package.json:31-32,52-54`, `biochemai/vite.config.ts:13`). Backend — Express + tsx on Node v26 (`biochemai/server.ts:1-2`, `biochemai/package.json:25,33`; `tsx` is in `dependencies`, satisfying Pattern 13 Rule A).
- **Auth**: Own Google button + WMS OAuth relay, plus a separate local username/password path. The Google button redirects to Google directly from the client (`biochemai/components/LoginView.tsx:20-38`); `server.ts` handles `GET /biochemai/callback` and relays the code→token exchange to `WMS_OAUTH_EXCHANGE_URL` (`biochemai/server.ts:22-23,86-130`), so no Google client secret is held here. Session state hydrates from a `biochemai_user` cookie into `AuthContext` (`biochemai/contexts/AuthContext.tsx:31-49`). Separately, `AuthService` implements a fully client-side username/password register/login that stores users and a fake token in `localStorage` only — there is no server-side account store (`biochemai/services/authService.ts:1-84`). UNVERIFIED: whether this local-account path is still reachable in the shipped UI or is dead code alongside the Google flow — not confirmed from the files read.
- **Gemini custody**: WMS relay (Pattern 11, `POST /api/gemini/generate`). `server.ts` never holds `GEMINI_API_KEY`; every call goes through `callGemini()` with `X-Gemini-Proxy-Key: GEMINI_PROXY_KEY` (`biochemai/server.ts:19-24,46-59`). Missing key returns HTTP 503, no `process.exit` (`biochemai/server.ts:27-29,47`). Confirmed also in `biochemai/CONSTRAINTS.md:61-79` ("Migrated from the transitional key-fetch mode on 2 Jul 2026"). Note: `PATTERNS.md:986` still lists biochemai under "Old pattern (do not copy): biochemai — direct key, fatal startup check" — that is stale; the current `server.ts` and `CONSTRAINTS.md` show the relay is already in place. Flagging this as a doc/reality mismatch inside `PATTERNS.md` itself (reality wins per the fleet rule).
- **Deploy**: `.\deploy.ps1 -Build` (there is no non-build path; `-Build` is implied by the script, see `biochemai/deploy.ps1:9`, param is `[switch]$Build` with no separate branch). Server-side clone + `vite build` + rsync `dist/` + `pnpm install` (no `--prod`, tsx must survive) + hard PM2 restart with the Node v26 wrapper (`biochemai/deploy.ps1:58-131,191-198`, Pattern 23). `.env` is rebuilt fresh each deploy with `GEMINI_PROXY_KEY` injected from `/opt/tuc-wms/.env` (Pattern 21 BOM-stripping) and aborts before restart if the key is missing (`biochemai/deploy.ps1:180-189`).
- **Known gotchas**:
  - Pattern 11 — WMS Gemini relay; missing `GEMINI_PROXY_KEY` degrades gracefully to HTTP 503, never a crash.
  - Pattern 13 — `tsx` correctly kept in `dependencies` (`biochemai/package.json:33`); deploy explicitly skips `--prod` (`biochemai/deploy.ps1:127-128`).
  - Pattern 21 — BOM stripping on the extracted `GEMINI_PROXY_KEY` value (`biochemai/deploy.ps1:184`).
  - Pattern 23 — hard `pm2 delete` + fresh `pm2 start` on every deploy, plus a banner-grep health check that the *new* build (not stale tsx cache) is actually serving (`biochemai/deploy.ps1:191-206`).
  - `CONSTRAINTS.md:77-79` documents known dead code: `components/voice/VoiceContainer.tsx` reads `process.env.API_KEY`, which is never injected by `vite.config.ts` — voice mode is non-functional but holds no key, so it is a functionality gap, not a security issue.
  - Code observation (not fixed, for the parent to action): the local username/password auth in `services/authService.ts` stores plaintext passwords and fake tokens entirely in browser `localStorage` — no server validation at all. If this path is still user-reachable it is a real security gap, distinct from the OAuth flow.
