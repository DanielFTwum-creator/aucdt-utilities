# CLAUDE.md — MOVED

> This copy was a stale duplicate and has been retired.
>
> The authoritative project instructions are the monorepo-root file:
> **`aucdt-utilities/CLAUDE.md`**.
>
> Do not edit this file. Edit the root file instead.

## Why this stub exists

An older duplicate of the project instructions lived at this path and had drifted
from the root. It disagreed with the root on several points that were causing
confusion:

- **Port table** — listed Glucose on 3001; the real port is 3006. Port truth now
  lives in `SERVER_PORTS.md` (reality, verified via `ss` + `/proc/cwd`), with
  `PORT-REGISTRY.md` as the intent ledger.
- **Gemini key handling** — its pattern library copy showed a client-side key,
  which violates the WMS sole-key-custody rule.

The full previous content remains in git history if it is ever needed.
