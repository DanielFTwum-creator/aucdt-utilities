# LyriaStream AIM — Python dependencies
# CPU-first. GPU extras are commented out — uncomment for gpu_full mode.

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.1
pydantic-settings==2.3.1
python-multipart==0.0.9
httpx==0.27.0

# ── AI / Audio models ─────────────────────────────────────────────────────────
torch==2.3.1                   # CPU build by default
torchaudio==2.3.1
transformers==4.41.2           # MusicGen via HuggingFace
diffusers==0.29.2              # Riffusion (Stable Diffusion variant)
accelerate==0.31.0
huggingface-hub==0.23.4
sentencepiece==0.2.0
scipy==1.13.1

# ── Audio processing ──────────────────────────────────────────────────────────
librosa==0.10.2                # Mel spectrograms, Griffin-Lim
soundfile==0.12.1              # WAV I/O
pyloudnorm==0.1.1              # LUFS loudness normalisation
numpy==1.26.4
Pillow==10.3.0                 # Riffusion spectrogram images

# ── Redis (SSE pub/sub, job queue) ────────────────────────────────────────────
redis==5.0.7
aioredis==2.0.1

# ── Observability ─────────────────────────────────────────────────────────────
prometheus-fastapi-instrumentator==7.0.0
structlog==24.2.0

# ── Dev/test ──────────────────────────────────────────────────────────────────
pytest==8.2.2
pytest-asyncio==0.23.7
httpx==0.27.0
pytest-cov==5.0.0

# ── GPU extras (uncomment for gpu_fast / gpu_full mode) ───────────────────────
# torch==2.3.1+cu121 --index-url https://download.pytorch.org/whl/cu121
# xformers==0.0.26.post1
