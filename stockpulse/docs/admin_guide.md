# Admin Guide — StockPulse

## Access
Navigate to `/#/admin` or click "Admin" in the footer.

**Password:** `admin123`

## Features
- Audit log of all navigation and search events.
- Diagnostics: verify market data proxy connectivity.

## Logs
Stored in localStorage key `stockpulse-audit`.

## Backend
Market data proxy runs on `backend/`. Start with `pnpm dev` from `backend/` directory.
