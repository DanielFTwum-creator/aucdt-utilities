# Hoisted (flat, npm-style) node_modules layout. pnpm v11's default symlinked
# layout fails to link esbuild's optional platform binary (@esbuild/win32-x64)
# on Windows, so vite build hangs with no esbuild binary. Hoisting resolves it.
node-linker=hoisted
